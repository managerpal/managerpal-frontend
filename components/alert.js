import {Button, SafeAreaView, Text} from "react-native";

const AlertScreen = () => {
    return (
        <SafeAreaView>
            <Text style={{
                textAlign: 'center',
                fontSize: 28,
                fontWeight: '500',
                marginBottom: 20
            }}>
                Alert page
            </Text>
        </SafeAreaView>
    )
}

export default AlertScreen